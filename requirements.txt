# 춤마루 MVP - 실시간 자세 감지 애플리케이션
# 최종 업데이트: 2026-01-01

# 웹 프레임워크
streamlit>=1.28.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# 컴퓨터 비전 및 MediaPipe
# Streamlit Cloud 배포용: opencv-python-headless (GUI 없음)
# WebRTC가 브라우저 카메라를 처리하므로 headless로 충분
opencv-python-headless>=4.8.0
mediapipe>=0.10.0

# 이미지 처리
Pillow>=10.0.0

# WebRTC (브라우저 카메라 접근 - Streamlit Cloud 필수)
# 상세: ISSUES_LOG.md [7], [8] 참조
streamlit-webrtc>=0.45.0
av>=10.0.0

# 피드백 자동 업데이트 (WebRTC 콜백 → 메인 스레드 동기화)
streamlit-autorefresh>=1.0.0

# 구글 시트 연동 (기존 기능용)
st-gsheets-connection>=0.0.3
